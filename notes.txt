1. add tasks route in client side
2. add tasks view
3. get user tasks
4. edit task title
5. create task
6. task disabled/archived state

todo: 
* make a task component

* add a route controller to add and remove child tasks (edit child tasks array)
* bread crumb component will traverse parent task uids and generate the breadcrumb with matching titles with each list load (tasks load)